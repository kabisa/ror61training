<% content_for(:page_title) do
  "Editing #{truncate(@post.title_was)}"
end %>
<%= render 'form', post: @post %>
<%= link_to t('helpers.show'), @post %> |
<%= link_to t('helpers.back'), posts_path %>
