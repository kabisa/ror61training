<% content_for(:page_title) { truncate(@post.title) } %>
<p id="notice"><%= notice %></p>
<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>
<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
<%= render partial: '/comments/comments', locals: { commentable: @post, new_comment: @comment } %>
